import { API_KEYS, API_ENDPOINTS } from '../../config/constants';
import { AzureConfig } from './types';

export const azureConfig: AzureConfig = {
  endpoint: API_ENDPOINTS.COMPUTER_VISION,
  apiKey: API_KEYS.COMPUTER_VISION
};